# Home-page background image

Save the aerial parking image supplied for this project in this folder with this exact filename:

```text
parking-lot-aerial.jpg
```

The Home page already uses `/images/parking-lot-aerial.jpg` as its responsive full-bleed background. A dark overlay keeps the title, buttons, and booking steps readable on top of the photograph.
